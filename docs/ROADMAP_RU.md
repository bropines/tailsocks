# План Развития Проекта (Roadmap)

Этот документ описывает планируемые функции, архитектурные улучшения и цели рефакторинга проекта TailSocks.

---

## ✅ Завершённые Этапы
- [x] **Стабильность ядра:** Пассивное управление демоном и декларативная конфигурация.
- [x] **Передача файлов:** Реализация Taildrop с интеграцией в Storage Access Framework.
- [x] **Изоляция профилей:** Мультиаккаунтная система с независимыми каталогами состояния.
- [x] **Сетевые возможности:** Кастомное DNS-обертывание и поддержка Exit Nodes.
- [x] **Tailscale Serve/Funnel:** Нативный UI для хостинга сервисов и публичного доступа.
- [x] **Local API v0:** Управление 100% без CLI через Go-HTTP мост.
- [x] **Интеграция с системой:** Базовая плитка Быстрых настроек (Quick Settings Tile) для переключения подключения.
- [x] **Мониторинг сервисов:** Проверки состояния (health checks) и логи запросов для размещенных сервисов Serve/Funnel.
- [x] **Поиск и фильтрация:** Функция поиска в списке узлов (Peers) и логах.
- [x] **Защита от энергосбережения:** Диалог помощи пользователю по выходу из ограничений батареи.
- [x] **Переключение аккаунтов в плитах:** Расширение плитки Быстрых настроек для переключения между профилями.
- [x] **Прокси панели управления:** Поддержка SOCKS5/HTTP прокси для трафика управления с атомарными патчами и фиксом обхода netns.
- [x] **Зашифрованные бэкапы:** Ручной бэкап состояния приложения, зашифрованный пользовательским паролем (AES-GCM) для защиты ключей узлов.
- [x] **Плитки Быстрых Настроек:** Плитки в шторке Android для управления переключением профилей и состояния подключения.
- [x] **R8-минификация (4.0.0):** `isMinifyEnabled`/`isShrinkResources` включены для релизных сборок. Gson заменён на `kotlinx.serialization`, сервис AppFunctions создаёт сгенерированные KSP реестры напрямую, а Gradle-задача `verifyReleaseNativeMethods` роняет сборку, если R8 удалил JNI-метод.
- [x] **Ужесточение Root-режима (4.0.0):** отдельные цепочки `TAILSOCKS_MARK`/`TAILSOCKS_DNS`, fwmark с маской `0x1000000/0x1000000`, диагностика «Проверить маршрутизацию» и вкладка ROOT в логах.
- [x] **Безопасность автоматизации (4.0.0):** broadcast receiver требует секретный токен; AppFunctions (Gemini, Android 16+) реально выполняются и подчиняются переключателю автоматизации.
- [x] **Доступ из LAN, автопереподключение, восстановление службы в фоне и версионированные бэкапы (4.0.0).**
- [x] **Сосуществование Root-режима с другими VPN (4.0.0):** набор правил разделён на уровни — доступность тайлнета, перехват маршрута по умолчанию, общесистемный редирект DNS, — причём у последних двух на устройстве только один владелец. Появилась частичная уступка: мы берём ровно те uid, которые чужой клиент вывел из своего туннеля, редирект DNS включается только пока MagicDNS отвечает, а интерфейсы с адресом внутри 100.64.0.0/10 больше не захватываются. Проверено на Redmi автора (APatch), в том числе после настоящей перезагрузки.
- [x] **Сообщать настоящую ОС (4.2.0, не выпущено):** выбор на уровне профиля, делается при его создании (онбординг, диалог добавления аккаунта; переключатель в Настройках — для профиля, который ещё не вошёл, иначе предлагается новый профиль). Выключено — маскировка патча 06: OS `linux`, App `tailscale-cli`, DeviceModel `Tailsocks`. Включено — узел регистрируется как OS `android` с настоящей моделью, версией Android и источником установки, как официальный клиент. Измерено 2026-09-07: такой узел получает `https`/`funnel`, сертификаты и рабочий Funnel; координатор отвергает только узел, сменивший ОС после регистрации.
- [x] **Root-режим после перезагрузки (4.2.0, не выпущено):** загрузочный скрипт теперь берёт каталог состояния активного профиля, адреса прослушивания SOCKS5/HTTP, учётные данные SOCKS5 и режим туннеля (ядерный TUN или userspace) из env-файла, принадлежащего root, запускает демон с той же командной строкой, что и приложение, и выставляет те же права на файлы (лог 644, сокет 666, каталог состояния 700). Строка `TS_VPN_BYPASS=0`, которую загрузочный скрипт раньше терял, записывается в кавычках, поэтому «Игнорировать другие VPN = выкл» переживает перезагрузку. Env-файл обновляется при каждом старте Root-режима, в том числе когда приложение присоединяется к демону, поднятому загрузочным скриптом.
- [x] **Падение демона больше не считается ручной остановкой (4.2.0, не выпущено):** флаг desired_running остаётся выставленным, если демон умер сам, поэтому автопереподключение перезапускает его в пределах лимита попыток; когда лимит исчерпан, служба уходит в отбой с уведомлением «нажмите, чтобы переподключиться», а 15-минутный сторож всё ещё может её оживить.

---

## 🔮 Планы

Состояние на 2026-09-07, после выпуска 4.1.0 и с невыпущенной 4.2.0.

### Крупное

- [ ] **Нативный TUN.** План: [`NATIVE_TUN_PLAN.md`](NATIVE_TUN_PLAN.md) (только на английском) —
      семь шагов (0–6, последний — необязательная полировка), после каждого приложение остаётся
      рабочим. Целевая версия: не назначена, назначить заново перед началом работы — план
      проскочил мимо 4.0.0 и 4.1.0, так что каждое «4.1» внутри него устарело. Главный риск снят:
      политика SELinux разрешает приложению `TUNGETIFF` на дескрипторе туннеля
      (`allowxperm untrusted_app tun_device chr_file ioctl { 0x54D2 }`, прочитано с Redmi).
      На устройстве по-прежнему не проверено: передача дескриптора в дочерний процесс через
      `SCM_RIGHTS`, поведение при смерти владельца и задержка при подмене. Для проверки нужен
      отладочный помощник внутри APK — через `su` проверять бессмысленно, это другой домен
      SELinux.
- [ ] **tsnet — идея для 5.0.** Демон внутрь процесса приложения. Несовместимо с Root-режимом,
      где он обязан быть отдельным процессом под `su`.
- [ ] **Отдельный CLI-бинарник.** Решение за автором, отложено. Цена измерена: около 6 МБ в
      каждом срезе по ABI, 25 МБ в универсальном APK и примерно 22 МБ на устройстве, потому что
      нативные библиотеки распаковываются при установке. Что он даёт: shell-обёртку Root-режима
      (`su -c tailscale …`) и горстку команд Консоли, у которых уже есть эквиваленты в LocalAPI.
  - **Выбрать из:** (a) оставить как есть — нулевая работа.
  - (b) Скачивать по требованию из собственного GitHub-релиза приложения — самый маленький APK, но это загрузчик, проверка хеша, публикация ассетов в CI, привязка версий и вопросы доступности, а скачанный бинарь, скорее всего, сможет запускать только root.
  - (c) Флейворы Gradle `lite` / `full` — изменение только сборки, но матрица вариантов удваивается, а за ней идут переименования в CI и поддержка в апдейтере.
  - (d) Класть CLI только в универсальный APK — тот же механизм, что и (c), страница релиза остаётся с пятью файлами (четыре APK по ABI плюс универсальный).
  - (e) Сборочный тег `ts_include_cli`, который предлагает апстрим: один бинарник в обеих ролях, `tailscaled` передаёт управление CLI, когда его вызвали как `tailscale`, — то есть второй библиотеки на 6 МБ не будет вовсе. Этого варианта в прежнем списке не было.
  - Замечание: `useLegacyPackaging` отключить нельзя — демон запускается через `exec()` из
    `nativeLibraryDir`, а для этого библиотеки должны быть распакованы на диск.

### Требует решения автора

- [x] **Честная ОС — что выяснило исследование (2026-09-07).** Патч 06 подставляет `OS = linux`
      исходя из того, что координатор игнорирует объявленные сервисы у андроидных узлов. Это не
      подтвердилось: в клиенте нет ни одной проверки `Hostinfo.OS` для serve, funnel или cert —
      единственные ворота это узловые capability `https` и `funnel`, приходящие в netmap.
      Настоящие андроидные ворота — сборочный тег `!android` на `ipn/localapi/cert.go`, и его уже
      снимает патч 05, то есть Serve/Funnel работали не благодаря маскировке. peerAPI и Taildrop
      не затронуты: узел `Pixel 8` с `OS = android` в этой сети объявляет `peerapi4`/`peerapi6`,
      а в коде отказ прописан только для tvOS. Единственное задокументированное последствие
      честности — device posture: `node:os` переключается с linux на android. Апстримный
      `tailscale/tailscale#18245` показывает узел с `OS = android`, получающий сертификаты от
      координатора. Сам переключатель уже сделан (см. «Завершённые этапы», по умолчанию выключен).
      **Измерено 2026-09-07 на Redmi:** в маскараде у узла есть `https` и `funnel`, Funnel отвечает
      из публичного интернета за 5 с, `tailscale cert` выдаёт сертификат, координатор забирает
      список сервисов по c2n; честно — для того же узла (зарегистрированного как Linux) координатор
      отвечает «node OS changed since last connection, was node state copied between devices?»,
      не отдаёт netmap и capabilities, `cert` падает. Значит, зарегистрированный узел
      сменить ОС не может. Вторая половина прошла в тот же день на новом профиле: узел, с самого
      начала зарегистрированный как Android, держит `https` и `funnel`, `tailscale cert` выдаёт
      сертификат, Funnel отвечает HTTP 200 из публичного интернета за 5 с, а консоль
      администратора называет машину по модели устройства (`xiaomi-23030rac7y`, «Android (16)»).
      Отсюда настройка — свойство профиля, фиксируемое при создании. Решено
      2026-09-07: для новых профилей по умолчанию выключено, маскировка остаётся нормой.
- [ ] **Задачи бота-сканера #5, #6, #7** — проверено 2026-09-07: `x/crypto/ssh` не попадает ни в один поставляемый бинарник (`ts_omit_ssh` плюс апстримное ограничение сборки `!android` на SSH-сервер), версию диктует закреплённый модуль апстрима, а оба PR меняют только `go.mod` моста. Закрывающий комментарий написан; публикует его автор (ассистенту запрещено писать в GitHub).
- [ ] **Задача #3** — запрос, с которого начался Root-режим. Автор уже ответил; закрывать или
      ждать подтверждения от `TheLastFlame` на его планшете.

### Проверка на устройствах

- [ ] **Root-режим на WSA после перезагрузки:** автозапуск через `service.d` и присоединение
      приложения к демону, которого оно не запускало.
- [x] **Эксперимент с честной ОС** — обе половины прошли 2026-09-07 (см. «Требует решения
      автора»). Не измерено: *тегированный* Android-узел как хост `svc:` (новый профиль был
      пользовательским, а демон отказывает в хостинге сервисов без тегов независимо от ОС).
- [ ] **Права принятых файлов в Root-режиме.** Правка сделана вслепую (`umask 022` плюс передача
      каталога приложению). В «Проверить маршрутизацию» теперь выводятся реальные права —
      посмотреть и подтвердить.
- [ ] **Утечка exit node по IPv6** на Redmi 2026-09-07 не воспроизводится: маршрут по умолчанию
      в таблице `52` есть, трафик уходит в туннель с тайлнет-адресом источника. Проверить на
      POCO, где сеть другая; если не воспроизведётся и там — вычеркнуть.
- [ ] **Версия пира из Admin API** уже реализована и работает только при настроенном токене.
      Осталось посмотреть на устройстве, где он настроен.
- Отложено, не реализовано — записано, чтобы снова не потерялось:
  - Чужой туннель, перезапустившийся с новым netId: сигнатура набора правил учитывает netId, но на устройстве этого никто не наблюдал.
  - Телефоны с FBE: дождаться CE-хранилища пользователя 0 перед стартом.

### Мелкое

- [ ] **IPv6 в перехвате DNS.** На ядре Redmi (4.19) таблицы `nat` для шестой версии не
      существует — писать правила некуда, это ограничение прошивки, а не наш пробел. Сделать:
      сказать об этом в диагностике вместо нынешнего молчания и проверить, не уходит ли
      DNS-запрос по IPv6 мимо MagicDNS.
- [ ] **Команды Консоли `status` / `netcheck` / `ping` не работают в Root-режиме.** Симлинк на
      CLI создаёт только userspace-путь запуска (`appctr/daemon.go`); `AttachExternal` его не
      создаёт вовсе, поэтому в Root-режиме этим командам нечего вызывать.
- [ ] **Пять индикаторов загрузки внутри кнопок** остались старыми — новый компонент при размере
      14 dp перестаёт читаться. Посмотреть на устройстве и решить.
- [ ] **Стабильная Material 3.** Сейчас `1.5.0-alpha27` ради компонентов, которых нет в 1.4.0, и
      это по-прежнему самая свежая версия — стабильного выпуска пока не существует. Выйдет —
      смена одной строки в `gradle/libs.versions.toml`.
- [ ] **Обновление Tailscale** с 1.102.1 на текущий выпуск апстрима, v1.102.3 (2026-08-20).
      Поправка к прежней записи: он *не* подтягивает версии зависимостей, о которых просил бот, —
      v1.102.3 всё ещё фиксирует `x/crypto` v0.54.0. Они появятся только с более поздним выпуском
      апстрима.
- [ ] **Английская версия пасхалки** — если делать, то другой шуткой, а не переводом.
- Отложено, не реализовано — записано, чтобы снова не потерялось:
  - Переустанавливать набор правил после того, как перезапуск netd их вычистил; сегодня на это ничто не реагирует.
  - Не убивать ещё стартующий демон, когда приложение его усыновляет, — сегодня там одна проба.
  - Версия в заголовке установленного загрузочного скрипта и состояние «устарел» в настройках.

### Аудит второго проекта

- [ ] **`bropines/tailscale-termux-cli`** — аудит запущен 2026-09-07, отчёт кладётся вне этого
      репозитория. Починка найденного — отдельной сессией из папки того проекта.

### Отброшено

- Отброшено сознательно, кода за этим нет: MCP-сервер, мост LocalAPI на Kotlin, анализатор
  трафика и пользовательские карты DERP.
