diff -N -r -u orig/drive/driveimpl/fileserver.go tailscale_src/drive/driveimpl/fileserver.go
--- orig/drive/driveimpl/fileserver.go	2026-05-21 21:27:19.000000000 +0300
+++ tailscale_src/drive/driveimpl/fileserver.go	2026-05-28 12:45:37.210267110 +0300
@@ -10,6 +10,7 @@
 	"fmt"
 	"net"
 	"net/http"
+	"strings"
 	"sync"
 
 	"github.com/tailscale/xnet/webdav"
@@ -113,7 +114,7 @@
 	defer s.UnlockShares()
 	s.ClearSharesLocked()
 	for name, path := range shares {
-		s.AddShareLocked(name, path)
+		s.AddShareLocked(strings.ToLower(name), path)
 	}
 }
 
@@ -147,7 +148,7 @@
 		return
 	}
 	r.URL.Path = shared.Join(parts[2:]...)
-	share := parts[1]
+	share := strings.ToLower(parts[1])
 	s.sharesMu.RLock()
 	h, found := s.shareHandlers[share]
 	s.sharesMu.RUnlock()
diff -N -r -u orig/drive/remote_android.go tailscale_src/drive/remote_android.go
--- orig/drive/remote_android.go	1970-01-01 03:00:00.000000000 +0300
+++ tailscale_src/drive/remote_android.go	2026-05-28 11:42:54.118149540 +0300
@@ -0,0 +1,13 @@
+// Copyright (c) Tailscale Inc & contributors
+// SPDX-License-Identifier: BSD-3-Clause
+
+//go:build android
+
+package drive
+
+func doAllowShareAs() bool {
+	// Android does not support running commands as different users (su/sudo)
+	// inside the app sandbox, so we force doAllowShareAs to return false.
+	// This makes Tailscale use the single file server instance directly.
+	return false
+}
diff -N -r -u orig/drive/remote_unix.go tailscale_src/drive/remote_unix.go
--- orig/drive/remote_unix.go	2026-05-21 21:27:19.000000000 +0300
+++ tailscale_src/drive/remote_unix.go	2026-05-28 11:42:58.526149638 +0300
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build unix
+//go:build unix && !android
 
 package drive
 
