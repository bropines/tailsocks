diff -N -r -u -x '*.orig' -x '*.rej' orig/drive/driveimpl/drive_test.go tailscale_src/drive/driveimpl/drive_test.go
--- orig/drive/driveimpl/drive_test.go	2026-07-31 19:22:18.000000000 +0300
+++ tailscale_src/drive/driveimpl/drive_test.go	2026-09-01 14:52:19.623363638 +0300
@@ -556,7 +556,7 @@
 	r := &remote{
 		ln:          ln,
 		fileServer:  fileServer,
-		fs:          NewFileSystemForRemote(log.Printf),
+		fs:          NewFileSystemForRemote(log.Printf, nil),
 		shares:      make(map[string]string),
 		permissions: make(map[string]drive.Permission),
 	}
diff -N -r -u -x '*.orig' -x '*.rej' orig/drive/driveimpl/fileserver.go tailscale_src/drive/driveimpl/fileserver.go
--- orig/drive/driveimpl/fileserver.go	2026-07-31 19:22:18.000000000 +0300
+++ tailscale_src/drive/driveimpl/fileserver.go	2026-09-01 14:52:19.623623591 +0300
@@ -10,6 +10,7 @@
 	"fmt"
 	"net"
 	"net/http"
+	"strings"
 	"sync"
 
 	"github.com/tailscale/xnet/webdav"
@@ -113,7 +114,7 @@
 	defer s.UnlockShares()
 	s.ClearSharesLocked()
 	for name, path := range shares {
-		s.AddShareLocked(name, path)
+		s.AddShareLocked(strings.ToLower(name), path)
 	}
 }
 
@@ -147,7 +148,7 @@
 		return
 	}
 	r.URL.Path = shared.Join(parts[2:]...)
-	share := parts[1]
+	share := strings.ToLower(parts[1])
 	s.sharesMu.RLock()
 	h, found := s.shareHandlers[share]
 	s.sharesMu.RUnlock()
diff -N -r -u -x '*.orig' -x '*.rej' orig/drive/driveimpl/remote_impl.go tailscale_src/drive/driveimpl/remote_impl.go
--- orig/drive/driveimpl/remote_impl.go	2026-07-31 19:22:18.000000000 +0300
+++ tailscale_src/drive/driveimpl/remote_impl.go	2026-09-01 14:52:19.623696829 +0300
@@ -27,27 +27,33 @@
 	"tailscale.com/drive/driveimpl/compositedav"
 	"tailscale.com/drive/driveimpl/dirfs"
 	"tailscale.com/drive/driveimpl/shared"
+	"tailscale.com/net/tsaddr"
+	"tailscale.com/net/tsdial"
 	"tailscale.com/safesocket"
 	"tailscale.com/types/logger"
 )
 
-func NewFileSystemForRemote(logf logger.Logf) *FileSystemForRemote {
+func NewFileSystemForRemote(logf logger.Logf, getDialer func() *tsdial.Dialer) *FileSystemForRemote {
 	if logf == nil {
 		logf = log.Printf
 	}
 	fs := &FileSystemForRemote{
 		logf:        logf,
 		lockSystem:  webdav.NewMemLS(),
+		getDialer:   getDialer,
 		children:    make(map[string]*compositedav.Child),
 		userServers: make(map[string]*userServer),
 	}
 	return fs
 }
 
-// FileSystemForRemote implements drive.FileSystemForRemote.
+// FileSystemForRemote is the drive filesystem exposed to remote nodes. It
+// provides a unified WebDAV interface to local directories that have been
+// shared.
 type FileSystemForRemote struct {
 	logf       logger.Logf
 	lockSystem webdav.LockSystem
+	getDialer  func() *tsdial.Dialer
 
 	// mu guards the below values. Acquire a write lock before updating any of
 	// them, acquire a read lock before reading any of them.
@@ -185,9 +191,18 @@
 					return nil, err
 				}
 
-				_, err = netip.ParseAddrPort(addr)
+				ipp, err := netip.ParseAddrPort(addr)
 				if err == nil {
-					// this is a regular network address, dial normally
+					// this is a regular network address
+					if tsaddr.IsTailscaleIP(ipp.Addr()) {
+						var dialer *tsdial.Dialer
+						if s.getDialer != nil {
+							dialer = s.getDialer()
+						}
+						if dialer != nil {
+							return dialer.UserDial(ctx, "tcp", addr)
+						}
+					}
 					var std net.Dialer
 					return std.DialContext(ctx, "tcp", addr)
 				}
diff -N -r -u -x '*.orig' -x '*.rej' orig/drive/remote_android.go tailscale_src/drive/remote_android.go
--- orig/drive/remote_android.go	1970-01-01 03:00:00.000000000 +0300
+++ tailscale_src/drive/remote_android.go	2026-09-01 14:52:19.623760824 +0300
@@ -0,0 +1,13 @@
+// Copyright (c) Tailscale Inc & contributors
+// SPDX-License-Identifier: BSD-3-Clause
+
+//go:build android
+
+package drive
+
+func doAllowShareAs() bool {
+	// Android does not support running commands as different users (su/sudo)
+	// inside the app sandbox, so we force doAllowShareAs to return false.
+	// This makes Tailscale use the single file server instance directly.
+	return false
+}
diff -N -r -u -x '*.orig' -x '*.rej' orig/drive/remote_unix.go tailscale_src/drive/remote_unix.go
--- orig/drive/remote_unix.go	2026-07-31 19:22:18.000000000 +0300
+++ tailscale_src/drive/remote_unix.go	2026-09-01 14:52:19.623791850 +0300
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build unix
+//go:build unix && !android
 
 package drive
 
